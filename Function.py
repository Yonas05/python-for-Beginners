# function : a block of code which is executed only when it is called
def hello(name, lat):
    print("hello mother fucker " + name, lat)


my_name = "Freaky dickhead"
lat = "Don't come home early, Okay"
hello(my_name, lat)
